async function test() {
    // We cannot run performance API exactly like browser in Node.js easily,
    // so I will just test the logic concept.
    console.log("Ping logic conceptually sound for browsers.");
}
test();
